# Meridian Space · 组件与动效参考基准

首页导航是全站唯一导航基准；About、文章页与案例页必须复用相同的组件结构和 `Work / Product / Side B / About / Resume` 顺序。Resume 独立为最右侧高对比胶囊按钮，并在 PDF 可用后以新标签页打开。首页紧凑页脚同时用于 About，案例页继续保留案例专用页脚。

> **用途**：本文件是网站的长期组件决策记录。任何新增、重做或替换首页／作品页／页尾的组件前，先读本文件，再读 `CASE-STUDY-GUIDE.md`。外部参考只用于提炼结构、状态与交互逻辑；不得把不同参考站的视觉语言直接拼贴进本站。

## 0. 本站先于外部参考

本站已有的基础规则不可被组件库覆盖：

- **页面底色**：首页使用 `--c-white: #FBFBF4`；案例页使用同一主底色 `--cs-paper: #FBFBF4`。禁止为了某个组件再造一层偏灰、偏黄的“近似米白”，从而制造页面断层。
- **强调色**：首页橙色 `--c-orange: #FF852D`；案例可以使用项目专属强调色，但同一页只保留一套强调逻辑。
- **容器**：所有内容与页尾对齐统一基于 `--gutter` 与既有内容容器，不能让一个新模块在同屏内自行另设左右边界。
- **动效曲线**：默认使用 `--ease: cubic-bezier(.16, 1, .3, 1)`；只有叙事明确需要“弹性回弹”时才用 spring。
- **卡片**：默认无描边、无阴影、无嵌套卡片。层级优先由留白、图片比例、标题和细分隔建立；可选项用轻微底色或选中态，而不是外框。
- **移动端**：必须按触控与阅读关系重排，不能把桌面多列原样垂直堆叠；整页不能产生横向滚动，横向滚动只能存在于明确可交互的组件内部。

## 1. 外部参考的取用结论

| 参考 | 可提取的能力 | 在本站的用法 | 结论 |
| --- | --- | --- | --- |
| [shadcn/ui Create](https://ui.shadcn.com/create) | 设计令牌、可配置基础组件、统一图标与字体选择 | 先定义语义变量与状态，再写组件；不直接搬 shadcn 的默认样式 | **用作系统方法** |
| [beUI Bouncy Accordion](https://beui.dev/components/motion/bouncy-accordion) | 单开手风琴、测量内容高度、可访问状态、减少动态偏好 | 首页 FAQ 与案例 disclosure 的交互基准 | **可直接复刻行为** |
| [BoardUI](https://www.boardui.com/components) | segmented control、tabs、slider、tooltip、social icon 等基础件的状态完整性 | 颜色切换、案例切换、比较器控制与页尾图标组 | **借用基础件逻辑** |
| [ThreeUI · Mira CRT Wall](https://threeui.com/backgrounds/mira-solvang-landing-page) | 独立、全幅、场景型 WebGL 背景的性能边界 | 仅在未来某个独立 Hero 确有叙事必要时使用 | **谨慎可选** |
| [Fluid Functionalism Card](https://www.fluidfunctionalism.com/docs/card) | 无边框卡片、共享容器、proximity hover、选中态去分隔线 | 作品入口、档案墙、选择组的视觉基准 | **核心视觉参考** |
| [Watermelon UI](https://ui.watermelon.sh/) | 可组合的内容模块、响应式状态 | 功能型信息组（而非其色彩和卡片外观） | **借用结构** |
| [HeroUI Pro](https://heroui.pro/) | 令牌、组件状态、设计到代码的一致性 | 组件命名与状态定义，避免一次性“特制 UI” | **用作系统方法** |

### 不应照搬的部分

- 不把任何参考站的渐变、强阴影、大圆角、深色面板或 dashboard 视觉直接加入本站。
- 不为“看起来高级”而引入 Three.js／WebGL。它只能服务一个独立场景，不能叠加在已有视频 Hero、滚动叙事和大量图片上。
- React 示例不能原样粘进当前静态 HTML 网站；应以当前 HTML、CSS、JavaScript 实现同等行为、语义和无障碍属性。

## 2. 全站组件语法

### 2.1 表面与容器

1. 默认表面就是页面本身，不再额外套一层卡片。
2. 需要归类时优先采用两种形式：
   - **共享框架**：一组相关项目置于同一浅色面内，内部只用 1px hairline 分隔；
   - **无边框独立块**：媒体、标题、说明靠间距与网格对齐。
3. 只有明确“当前被选择”的内容可使用低对比填色；选中块周边的分隔线应弱化或消失，让填色干净读取。
4. 不使用“卡片外面再套描边”“卡片内再套卡片”“同一行三种不同卡片材质”。

### 2.2 文本与对齐

- 编号／眉题／状态：14px；辅助说明／图注：16px；正文：18px / 1.8；字段标题：18px / 600。
- 标题占用真实内容宽度，自然断行；禁止单字成行。不能为造型提前塞 `<br>`。
- 一组内左、中、右元素应分别对齐同一内容容器的左边、视觉中心、右边；不能因一侧文字切换而让中间图标跳动。

### 2.3 动效分级

| 等级 | 适用内容 | 规则 |
| --- | --- | --- |
| A · 微反馈 | hover、按钮、图标、标签 | 160–240ms；仅 opacity、color、transform；不造成排版位移 |
| B · 内容展开 | FAQ、说明折叠、切换面板 | 280–560ms；先保持可读、再做空间变化；单次只打开一个同级项 |
| C · 叙事转场 | Hero 点亮、Before/After、档案墙 | 必须解释前后关系；可被滚动或点击中断；完成后把控制权还给页面 |

所有动效必须：

- 支持 `prefers-reduced-motion: reduce`，改为即时状态切换；
- 不以 `height: 100vh` 或空 spacer 凑时长；
- 不阻塞用户继续阅读；
- 在 390px 宽度下独立验收，不把桌面手势复制到手机。

## 3. 可复用组件规范

### A. 全站 Work 下拉作品入口

**来源**：Fluid 的“共享框架 + 媒体主导”与 BoardUI 的菜单状态。

- 桌面端悬停 / 键盘聚焦 Work 后展示；鼠标可从触发器连续移动到面板，不闪退。
- 左列为普通作品列表（缩略图、标题、编号）；右列为两项精选大图。现有结构中右侧优先为“文化墙展厅”“DMHC 特装展”。
- 面板是一个共享容器，不给每个项目另加外框；图像区域可有很轻的圆角，文本区域保持平面。
- 焦点可见、Esc 关闭、离开面板延迟关闭；手机端改为点击展开的全宽列表，不能依赖 hover。

### B. 图文作品入口 / 档案墙

**来源**：Fluid Functionalism Card。

- 图片是主角，标题、项目类型、编号为附属信息。
- hover 使用轻微位移或图片裁切变化；不加漂浮阴影，不同时做缩放、旋转、光晕三种效果。
- 大量作品优先做“共享网格 + 筛选状态”或滚动叙事；不能以两个巨大卡片隐藏其余作品，不能让访客误以为作品只有两张。
- 点击后使用站内链接或统一 lightbox；同一页面不可混用不同的放大交互。

### C. 分段控制（色票、案例 1/2、产品 1/2）

**来源**：BoardUI segmented control。

- 用于切换同一证据组的内容，控件本身必须在媒体上方或下方紧邻出现，明确当前状态。
- 桌面可配合拖动比较器；手机端案例与产品先用胶囊切换组选择对象，再在当前对象内拖动 Before/After，避免“横向切图”与“横向拖动比较”抢手势。
- 每个按钮至少 44px 触控目标，支持键盘方向键与 `aria-pressed` / `aria-selected`。

### D. Before / After 比较器

**来源**：本站现有 `.dmhc-compare`，继续作为统一实现。

- 一次只说明一件可看见的差异；标签必须明确为“设计稿 / 现场”或“Before / After”。
- 桌面：图上可拖动、下方 range 可精确操作；拖动应该连续，不跳跃。
- 手机：当前案例由 C 组件选择；比较器本身只接管横向拖动，页面仍可纵向滚动。
- 滑块轨道扁平、短、对比充分，不使用浏览器原始粗滚动条外观。

### E. 手风琴 / 深读内容

**来源**：beUI Bouncy Accordion。

- 默认单开，可全部收起；标题、编号、加号在同一行；答案与问题左对齐。
- 高度通过真实内容测量或 grid 过渡实现，不能固定写死高度。
- 动效建议：展开 480–560ms、收起 360–460ms，使用 `var(--ease)` 为主；“bouncy”只保留极轻的回弹，不让信息型 FAQ 像玩具。
- 关闭项应移除可聚焦内容（`inert` 或等效处理）；按钮维护 `aria-expanded` 与 `aria-controls`。
- FAQ 最后一项后不加多余分割线；每次展开只改变自身高度，不能带动页尾社交组水平跳动。

### F. Hero / 视频叙事

**来源**：现有 cinematic Hero；ThreeUI 只做性能边界参考。

- Hero 的第一帧必须有高质量静态 poster，避免出现灰色空白或视频未载入界面。
- 滚动驱动视频时使用原视频，不转码、不压缩；播放位置与滚动进度绑定，叠加的文字必须与视频内的实际屏幕透视和位置一起运动，不能漂浮在整页坐标系。
- 当视频内米白屏幕放大至铺满视口时，下一段米白内容应在原位以 opacity 0 → 1 出现，不用向上推挤转场。
- WebGL 场景如被采用：必须是独立 Hero、静态 poster 兜底、低性能/移动端禁用、离屏暂停、`prefers-reduced-motion` 禁用；不可与视频帧同步和其它大型视觉叠加。

### G. 页尾的联系、社交与 AI 入口

**来源**：HeroUI 的统一状态设计 + BoardUI 的 icon button 规则。

- 三个区块（联系方式、社交、AI）是同一横向组件，不是三个不同材质的卡片。
- 使用固定三列网格：左对齐联系方式、中列始终视觉居中、右对齐 AI 入口；邮箱/电话切换不得改变中列位置。
- 邮件展示采用紧凑的复制字段，去掉“发邮件”主按钮与额外外卡片；社交图标以可点击链接为主，尺寸一致、颜色使用品牌官方色。
- FAQ 和页尾使用同一个内容容器左右边缘，不得向页面边界漂移。

## 4. 实现前的检查清单

在写任何新增 UI 前逐项确认：

1. 它解释什么内容或帮助什么任务？如果没有明确答案，不做动效。
2. 它是否复用现有 A–G 中的某一组件，而非再造近似组件？
3. 默认、hover、focus、active、disabled、loading、空内容、错误以及 reduced-motion 状态是否都有定义？
4. 390px 手机端的手势是否只有一种明确职责？是否完全没有整页横向溢出？
5. 标题是否无单字成行？正文、图注、目录、页尾是否按统一字号与内容边缘对齐？
6. 是否避免了描边、阴影、圆角卡片的重复堆叠？
7. 图片、视频、WebGL 是否有加载策略和静态兜底？是否只加载首屏与当前可见资源？
8. 修改后是否已上线正式站，并分别在 PC 和 390px 手机端真实验证？

## 5. 推荐优先级

1. **先统一基础件**：Work 下拉入口、分段切换、FAQ、页尾三列对齐。
2. **再统一作品页交互**：比较器与移动端 Tab 选择逻辑、深读手风琴、媒体 lightbox。
3. **最后做场景型动效**：只有在叙事需要且性能预算允许时，才做视频 / WebGL Hero。

这份顺序意味着：先建立稳定、可访问、可扩展的组件语言；动效作为内容关系的表达，而不是额外堆叠的装饰。
