# 作品详情页设计基准

本规范以“昂楷科技产品单页系列”详情页为基准，适用于后续所有作品详情页。它统一的是叙事方法、阅读层级、交互原则和质量底线，不要求不同项目复用同一种版式。

## 为什么这个页面成功

### 1. 面试官可以快速复述项目

页面只有四个核心章节，每章标题本身就是一个完整论点：

1. 产品足够强，却很难被快速读懂。
2. 我做的第一件事不是设计，是写制度。
3. 一套骨架，十三种色彩身份。
4. 半年二十六个版面全线落地。

用户只扫标题和主图，也能理解问题、方法、系统和结果。正文用于证明结论，而不是承担全部叙事。

这里的“四章”是这个项目经过内容取舍后得到的结果，不是后续页面必须套用的固定模板。真正需要复用的是“每章都有独立论点、扫标题可以理解项目”的原则。

### 2. 同时服务三种阅读深度

- **10 秒判断**：Hero、章节论点、关键数字和代表作品说明项目价值。
- **1 分钟扫读**：每章的主视觉、粗体判断、结果证据构成完整故事。
- **深度阅读**：制度文件、时间线、正反面、完整档案和项目反思按需展开。

页面没有强迫所有人走完同一条长路径。信息披露、放大查看和手风琴把细节放在需要它的位置。

### 3. 作品拥有视觉主权

图片不是正文的插图，而是章节的主体。大幅作品、Before/After、色彩联动、正反面翻转和展厅档案墙都直接展示设计成果，文字退到解释设计决策的位置。

### 4. 交互负责解释观点

- Before/After 滑块解释“为什么需要重构”。
- 色票与单页联动解释“一套骨架，十三种身份”。
- 滚动翻面解释“先吸引，再承接专业信息”。
- 档案墙解释“系统已经完整交付并进入现场”。
- 手风琴让反思先以结论出现，感兴趣后再深读。

交互不是装饰，每一个动作都在帮助用户理解一个设计判断。

### 5. 证据链完整

页面从市场问题、竞品调研、制度文件、协作模板、视觉系统一路讲到邮件发布和展会现场。它证明的不只是“做了好看的单页”，而是设计师完成了问题定义、组织协作、系统设计和业务落地。

### 6. 视觉语言克制

页面主要依靠字号、留白、图片比例、颜色和运动建立层级，不依赖重复的圆角卡片、描边和嵌套容器。米白背景保持连续，橙色只承担定位与状态提示。

### 7. 移动端是重新编排，不是简单堆叠

移动端保留对比、翻面、档案墙和手风琴的核心关系，同时改用适合触控和窄屏的布局。横向内容由真实溢出驱动，高度由内容决定，不使用固定整屏占位制造滚动距离。

## 已验证的设计偏好

以下模式已经在“昂楷科技产品单页系列”详情页中通过实际体验验证，并获得明确认可。后续项目内容匹配时应优先考虑，但不要求每个页面全部使用；交互数量必须服从叙事，而不是反过来填满页面。

1. **Before/After 左右对比**：让传统方案与改造结果同时占据视觉主位，通过拖动直接看见信息结构和视觉语言的差异。移动端必须保留对比关系，不能退化为两张图片的普通纵向堆叠。
2. **针对手机端重新设计交互**：窄屏不是桌面版的缩小副本。应根据触控、可视面积和阅读节奏重新组织切换方式，同时保留桌面端观点与证据之间的对应关系。
3. **有叙事意义的深浅色切换**：深色模式或色彩切换可以建立阶段、主题与情绪差异，但必须与内容状态联动，不能只作为装饰性的换肤功能。
4. **把多张证据整合成一个观点**：“模板也是制度的一部分”将四张文件整合为一个证据组，比四个独立卡片更紧凑，也更容易看懂文件之间共同证明了什么。
5. **颜色联动与滑动叙事**：用单一主预览承接多种色彩身份，滚动或点按切换内容，让“一套骨架、十三种身份”成为可以直接体验的系统关系。
6. **正反两面的滚动驱动叙事**：让作品随阅读进度从正面过渡到背面，并同步切换解释文本；用户不用额外学习操作，就能理解两个面的不同任务。
7. **横向里程碑时间轴**：“半年二十六个版面全线落地”把五个关键节点及证据缩略图压缩在一条时间线上，既保留项目跨度，又允许按需点击放大，避免复杂时间线长期占据页面。
8. **虚拟亚克力档案墙**：这是用户提出的原创展示概念。墙面、亚克力层、投影、展位与地面共同构成交付现场；选中作品时仍保留空间背景，从而同时表达完整规模与单件细节。
9. **极简手风琴**：先展示可扫读的反思结论，展开后再提供论证。它能减少长文压力，也让多个判断保持清晰的平级关系。

这些模式成功的共同原因，是它们在不牺牲信息完整性的前提下压缩了认知负担：面试官先看作品与结论，感兴趣后再进入证据和细节。后续页面应优先复用这种“先理解、再探索、按需深读”的体验逻辑，而不是机械复制具体外观。

## 后续页面的叙事组织

每个新项目先根据材料本身提炼能够独立成立的章节论点，再开始排版。章节数量和顺序由项目复杂度、证据类型与最强亮点决定，不得为了符合四幕式而删掉必要内容，也不得为了凑结构硬塞章节。

以下内容是可选择、合并和重新排序的叙事模块，不是固定目录：

1. **Hero**：一句项目价值、角色、范围和最有说服力的视觉作品。
2. **问题重构**：业务背景、用户或市场问题，以及设计任务如何被重新定义。
3. **关键方法**：项目最独特的决策或工作方式，不使用通用流程凑章节。
4. **系统与作品**：让核心作品获得最大视觉面积，并用交互解释系统关系。
5. **落地与结果**：真实发布、使用现场、数字结果和可验证证据。
6. **反思与收束**：三到四个可迁移判断，使用手风琴或短论点，不重复最终总结。

四幕式可以作为简单项目的优先起点，但不是上限。项目确实需要更完整的叙事时，可以适当增加章节；新增章节必须引入新的关键判断、证据或转折，并且在扫读层仍然清晰。无法形成独立论点的内容应合并、折叠或删除，避免把大量同权重小节平铺成网页化文档。

## 统一的阅读层级

### 第一层：结论

- Hero 和章节标题必须在脱离正文时仍能讲清故事。
- 章节标题写判断，不写“项目背景”“设计过程”这类目录词。
- 每章只保留一个最重要的观点。

### 第二层：证据

- 每个观点紧跟一个最有说服力的作品、数据或真实文件。
- 数字只出现一次，放在冲击力最强的位置。
- 相同结构的对比组件只保留一处，避免重复证明同一件事。

### 第三层：细节

- 长说明放入 disclosure、lightbox、tooltip 或手风琴。
- 图名、放大提示和脱敏说明使用统一图标与悬浮信息，不进入正文层级。
- 正文优先采用“加粗判断 + 灰色补充”，连续段落不超过两段。

## 视觉与排版规则

- 用户明确要求保留原始视频质量时，资源必须直接复制，不得转码、压缩、降分辨率或降码率；接入前后用 SHA-256 校验文件完全一致。
- 作为主内容的宣传片进入主要视区后可静音自动播放；暂停状态必须在画面中央提供尺寸清晰的播放按钮，不能只依赖浏览器控制条角落里的小图标。离开视区后自动暂停。

- 段与段、章节与章节之间只用统一留白建立边界，默认至少 `64px`，不添加横向分割线；分割线只用于同一段内部仍需区分的并列条目，且最后一项后不加线。相邻章节只保留一份间距，不得叠加前段底部与后段顶部留白。
- 字体层级按**语义角色**统一，不得按页面模块、背景颜色或临时视觉感觉各写一套字号。
- 桌面端基础字号固定为三档：编号／眉题／状态标签 `14px`，字段内容／图注／辅助说明 `16px`，正文 `18px / 1.8`。字段标题属于信息骨架，统一使用 `18px / 600`。
- 相同层级必须使用相同字号和字重；深浅背景、强调与非强调状态只允许改变颜色，不得通过缩小字号或减轻字重制造差异。
- 标题使用统一的响应式比例；同一级标题必须复用同一标题类或同一组字体声明，不允许为单个模块另设近似但不同的字号。
- 标题和副标题默认使用所在盒子的完整真实宽度，不添加人为 `max-width`，也不为造型过早插入 `<br>`；只有文字确实超过盒子可用宽度时，才由浏览器自然换行。若一句话在目标桌面宽度下无论如何都会稳定占两行，可在语义停顿处主动换行，但每一行仍须尽量撑满真实内容盒子，禁止留下突兀的大块空白。
- 手机端可缩小大标题，但正文不得低于 `16px`，图注和可阅读辅助信息不得低于 `16px`；只有方向刻度、纯编号等非阅读型界面元素可以低于 `14px`。
- 禁止在同一案例页正文中混入无明确语义依据的 `11px`、`12px`、`13px`、`15px` 临时字号。
- 正文阅读宽度控制在约 `34em`，避免宽屏长行。
- 大图居中并保留合理图间距；默认预览可裁切，放大后显示完整作品。
- 不使用卡片套卡片，不用描边代替层级，不为每段文字添加独立容器。
- 项目专属颜色可以覆盖站点强调色，但一个页面只保留一套主强调逻辑。
- 深浅背景切换必须服务叙事，不得形成黑、白、黑的无意义割裂。

## 交互选择规则

每个核心观点最多选择一个主交互。先问“这个动作解释了什么”，再决定是否实现。

| 叙事目的 | 推荐交互 |
| --- | --- |
| 展示改造差异 | Before/After 滑块；移动端使用 Tab 或可触控滑块 |
| 展示一套系统的多个变量 | Sticky 预览 + 色票或缩略图带 |
| 展示正反、前后或阶段变化 | 滚动驱动切换，保留点击和键盘兜底 |
| 展示完整交付规模 | 档案墙、展厅或可探索网格 |
| 展示里程碑和证据 | 桌面横向时间线，移动端纵向时间线 |
| 展示可选深读内容 | disclosure 或极简手风琴 |
| 查看作品细节 | 统一 lightbox，不重复“点击放大”文字 |

不允许用固定 viewport 高度、写死占位或与内容无关的 pin spacer 制造交互时长。需要 pin 时，滚动距离必须由真实内容尺寸或真实横向溢出计算，并在 resize 与 breakpoint 切换时安全销毁和重建。

### 横向滚轮轨道的固定方案

当桌面端需要“纵向滚轮驱动横向卡片”时，默认复用文化墙案例的紧凑轨道方案：

- 轨道保持真实内容高度，下面的正文或图片紧接出现；**严禁**增加纵向 spacer、`100vh`、整屏 `min-height` 或 sticky 长舞台来换取横向滚动距离。
- 只有指针位于轨道组件内、且轨道尚未到达对应方向的端点时，才捕获 `wheel`，将 `deltaY` 映射到横向位移。
- 到达首项或末项后必须立即停止 `preventDefault()`，把滚轮归还页面；反向滚动必须能从末项完整返回首项。
- 桌面初始可露出下一项的一部分以提示可继续浏览；移动端改为原生横向滑动或纵向排列，不复用桌面滚轮拦截。
- 必须提供键盘左右方向键兜底，并在 resize / breakpoint 切换时重新计算真实距离 `scrollWidth - clientWidth`。

上线前必须用真实浏览器完成四项验收：组件高度等于真实内容高度、下一板块间距正常、正向到达末端、反向返回起点；同时确认控制台为 0 error。只检查 CSS 数值、资源版本或脚本语法不算交互验收。

## 移动端规则

- 以 `390 x 844` 为基础验收尺寸，同时检查更窄和更宽设备。
- 导航保持紧凑，不挤占首屏主要叙事。
- 双栏关系不能无脑纵向堆叠；根据内容改为 Tab、横向滑动或主次切换。
- 桌面端一排多张的同级卡片，手机端默认改为原生横向滑动并露出下一张作为操作提示；只有存在明确线性阅读顺序时才允许纵向堆叠。
- 全景或大图上的长说明在手机端不得持续覆盖画面：默认只显示编号与标题，详细文案通过明确的展开按钮按需显示，展开后必须可收起。
- 手机端全景序列必须退化为稳定的单张轮播：图片使用 `contain` 完整显示，拖动松手后只切换相邻一张并自动归位，禁止停留在两张图片各露一半的中间状态。
- 手机端所有横向浏览组使用 `scroll-snap`、真实内容宽度和触控滚动，不套用桌面端的滚轮拦截、transform 位移或固定高度占位。
- 章节切换项较多时优先使用横向可滑动的紧凑 Tab，当前项必须清晰可辨，触控面积不得小于 `44px`。
- 图片预览优先使用统一比例，点击后查看完整原图。
- 可横向浏览的文档使用 `scroll-snap`，不得产生整屏空白或抖动。
- 自动横移只用于确有叙事必要的轨道，滚动距离等于真实 `scrollWidth - clientWidth`。
- 触控目标不小于约 `44px`，所有交互提供清晰状态和键盘兜底。
- 每个章节从标题到下一章节的垂直节奏统一，不使用临时高度补空白。

## 禁止回退的反模式

- 十个以上章节使用同一套编号、标题、长文和圆角卡片反复排列。
- 为了“丰富”而重复数字、重复对比或重复结论。
- 把视觉作品缩成正文旁边的小配图。
- 卡片嵌套、到处描边、深色脏背景和无意义序号。
- 用 `innerHeight`、固定 `min-height` 或 pin spacer 填补内容高度。
- 手机端把横向交互成本直接转嫁给用户，却没有明显滑动提示或自动叙事理由。
- 章节标题与正文之间、章节与章节之间随意添加分割线。
- 为每张图重复显示“点击放大”等正文式提示。

## 新页面验收清单

### 叙事

- [ ] 只读 Hero 和章节标题，能否在 30 秒内复述项目？
- [ ] 每个章节是否只有一个明确论点？
- [ ] 最强成果是否在前一分钟内出现？
- [ ] 数字、对比和结论是否没有重复？

### 视觉

- [ ] 核心作品是否获得足够面积和居中展示？
- [ ] 是否去除了不必要的卡片、描边、序号和容器嵌套？
- [ ] 是否先列出整页所有字号，再确认它们只落在规定的语义层级中？
- [ ] 所有同级字段、正文、图注和标签是否具有相同字号与字重，仅颜色不同？
- [ ] 桌面端是否遵循 `14 / 16 / 18px` 基础字号体系，字段标题是否统一为 `18px / 600`？
- [ ] 手机端正文与可阅读辅助文字是否均不低于 `16px`？
- [ ] 是否不存在无明确理由的 `11 / 12 / 13 / 15px` 临时字号？
- [ ] 标题和副标题是否使用盒子真实宽度，仅在确实溢出时自然换行？
- [ ] 正文、标题、注解和章节间距是否遵循统一层级？
- [ ] 页面是否有密度变化和视觉休息点？

### 交互

- [ ] 每个交互是否解释了一个具体观点？
- [ ] 点击、触控、键盘和 reduced-motion 是否都有可用路径？
- [ ] resize 后是否无重复实例、残留 spacer 或抖动？
- [ ] 横向滚轮轨道是否没有纵向占位，且下个板块紧接出现？
- [ ] 横向轨道是否真实滚到末端、再反向返回起点，并在两端归还页面滚动？
- [ ] 所有展开、翻转和放大状态是否有正确的 ARIA 状态？

### 移动端

- [ ] 是否逐段滚动检查完整页面，而非只检查首屏？
- [ ] 全景图、横向卡片、章节 Tab、对比滑块、视频等交互是否逐一完成触控测试？
- [ ] 长说明是否默认不遮挡主体画面，并能正常展开、滚动和收起？
- [ ] 多卡片是否露出下一项，且能从第一项滑到最后一项？
- [ ] 是否不存在接近整屏的无意义空白？
- [ ] 是否不存在横向页面溢出、裁字或导航遮挡？
- [ ] 内容增删后，高度是否仍由真实内容自动决定？

### 工程

- [ ] `node --check` 通过所有修改过的脚本。
- [ ] `git diff --check` 通过。
- [ ] 桌面 `1440px` 与手机 `390 x 844` 均完成真实浏览器验证。
- [ ] 部署后使用正式域名复查资源版本和关键交互。

## 使用方式

新作品页仍以 `css/case.css` 和 `js/case.js` 为基础。项目专属叙事可以增加独立的 CSS/JS 层，但必须复用本规范的阅读策略与验收标准，不能把本页面的所有组件机械复制到每个项目中。
