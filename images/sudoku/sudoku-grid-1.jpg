<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no, viewport-fit=cover"
    />
    <!-- SEO (source of truth: @atom63/brand/seo → endpoint "os63") -->
    <title>Meridian OS | Meridian</title>
    <meta
      name="description"
      content="Meridian OS — A web-based desktop environment by Meridian, exploring spatial UI, window management, and expressive desktop interactions."
    />
    <meta
      name="keywords"
      content="Meridian, Meridian, web desktop, desktop environment, spatial UI, window management, design engineer, visual systems"
    />
    <meta name="author" content="Meridian" />
    <meta name="robots" content="index, follow" />
    <meta name="theme-color" content="#000000" />

    <!-- Open Graph -->
    <meta property="og:type" content="website" />
    <meta property="og:url" content="https://chaoshanai.com" />
    <meta property="og:title" content="Meridian OS — Web Desktop by Meridian" />
    <meta
      property="og:description"
      content="Meridian OS — A web-based desktop environment by Meridian, exploring spatial UI, window management, and expressive desktop interactions."
    />
    <meta property="og:image" content="https://chaoshanai.com/og.png" />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta property="og:image:alt" content="Meridian OS" />
    <meta property="og:site_name" content="Meridian" />
    <meta property="og:locale" content="en_US" />

    <!-- Twitter -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content="Meridian OS — Web Desktop by Meridian" />
    <meta
      name="twitter:description"
      content="Meridian OS — A web-based desktop environment by Meridian, exploring spatial UI, window management, and expressive desktop interactions."
    />
    <meta name="twitter:image" content="https://chaoshanai.com/og.png" />
    <meta name="twitter:site" content="Meridian" />
    <meta name="twitter:creator" content="Meridian" />

    <!-- Favicons -->
    <link rel="icon" type="image/png" href="/favicons/favicon-96x96.png" sizes="96x96" />
    <link rel="icon" type="image/png" href="/favicons/favicon-48x48.png" sizes="48x48" />
    <link rel="icon" type="image/png" href="/favicons/favicon-32x32.png" sizes="32x32" />
    <link rel="icon" type="image/svg+xml" href="/favicons/favicon.svg" />
    <link rel="shortcut icon" href="/favicons/favicon.ico" />
    <link rel="apple-touch-icon" sizes="180x180" href="/favicons/apple-touch-icon.png" />
    <link rel="manifest" href="/favicons/site.webmanifest" />

    <!-- Preconnect to API domains -->
    <link rel="preconnect" href="https://api.notion.com" />
    <link rel="preconnect" href="https://api.github.com" />
    <link rel="preconnect" href="https://api.openai.com" />
    <link rel="preconnect" href="https://api.discogs.com" />

    <!-- Google Fonts: Geist + Geist Mono + Brawler + Doto + Instrument Serif (non-blocking) -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Brawler:wght@400;700&family=Doto:wght@100..900&family=Geist:wght@100..900&family=Geist+Mono:wght@100..900&family=Instrument+Serif:ital,wght@0,400;0,600;1,400;1,600&display=swap"
      rel="stylesheet"
      media="print"
      onload="this.media = 'all'"
    />
    <noscript>
      <link
        href="https://fonts.googleapis.com/css2?family=Brawler:wght@400;700&family=Doto:wght@100..900&family=Geist:wght@100..900&family=Geist+Mono:wght@100..900&family=Instrument+Serif:ital,wght@0,400;0,600;1,400;1,600&display=swap"
        rel="stylesheet"
      />
    </noscript>
    <!-- Stale bundle detection: reload if cached index.html references missing JS bundles -->
    <script>
      ;(function () {
        var reloadKey = 'os63:stale-reload'

        // After a successful recovery redirect (?_cb= present), clear the
        // cooldown flag so listeners get a fresh attempt.
        try {
          if (window.location.search.indexOf('_cb=') !== -1) {
            sessionStorage.removeItem(reloadKey)
          }
        } catch (e) {}

        var isReloading = false

        // <script> tag load errors (capture phase)
        window.addEventListener(
          'error',
          function (event) {
            if (isReloading) return
            var target = event.target || event.srcElement
            if (target && target.tagName === 'SCRIPT') {
              var src = target.src || ''
              if (src.indexOf('/assets/') !== -1 && src.indexOf('.js') !== -1) {
                triggerStaleReload()
              }
            }
          },
          true
        )

        // Dynamic import / module failures
        window.addEventListener('unhandledrejection', function (event) {
          if (isReloading) return
          var reason = event.reason
          if (!reason) return
          var message = reason.message || String(reason)
          var stack = reason.stack || ''
          var isModuleError =
            message.indexOf('dynamically imported module') !== -1 ||
            message.indexOf('Loading module') !== -1 ||
            message.indexOf('module script failed') !== -1 ||
            message.indexOf('Unable to preload CSS') !== -1
          var isAssetError =
            (message.indexOf('/assets/') !== -1 && message.indexOf('.js') !== -1) ||
            (stack.indexOf('/assets/') !== -1 && stack.indexOf('.js') !== -1)
          if (isModuleError || isAssetError) {
            triggerStaleReload()
          }
        })

        function triggerStaleReload() {
          if (isReloading) return
          // Reload loop guard (shared keys with reload-guard.ts)
          var countKey = 'os63:reload-count'
          var windowKey = 'os63:reload-window-start'
          try {
            var now = Date.now()
            var count = parseInt(sessionStorage.getItem(countKey) || '0', 10)
            var wStart = parseInt(sessionStorage.getItem(windowKey) || '0', 10)
            if (wStart && now - wStart <= 60000 && count >= 3) return
            if (!wStart || now - wStart > 60000) {
              sessionStorage.setItem(windowKey, String(now))
              sessionStorage.setItem(countKey, '1')
            } else {
              sessionStorage.setItem(countKey, String(count + 1))
            }
          } catch (e) {}
          // Cooldown check (10s)
          try {
            var lastReload = sessionStorage.getItem(reloadKey)
            if (lastReload && Date.now() - parseInt(lastReload, 10) < 10000) return
          } catch (e) {}
          isReloading = true
          try {
            sessionStorage.setItem(reloadKey, String(Date.now()))
          } catch (e) {}
          // Clear caches and SW, then reload
          if (window.caches) {
            caches
              .keys()
              .then(function (names) {
                return Promise.all(
                  names.map(function (n) {
                    return caches.delete(n)
                  })
                )
              })
              .then(doReload)
              .catch(doReload)
          } else {
            doReload()
          }
        }

        function doReload() {
          if (navigator.serviceWorker) {
            navigator.serviceWorker
              .getRegistration()
              .then(function (reg) {
                if (reg) reg.unregister().then(forceReload).catch(forceReload)
                else forceReload()
              })
              .catch(forceReload)
          } else {
            forceReload()
          }
        }

        function forceReload() {
          var url = new URL(window.location.href)
          url.searchParams.set('_cb', Date.now())
          window.location.replace(url.toString())
        }
      })()
    </script>
    <!-- Prevent mobile scrolling and ensure full viewport height -->
    <style>
      html,
      body {
        position: fixed;
        width: 100%;
        height: 100%;
        overflow: hidden;
        overscroll-behavior: none;
        -webkit-overflow-scrolling: touch;
        touch-action: none;
      }

      #root {
        position: fixed;
        inset: 0;
        width: 100%;
        height: 100%;
        overflow: hidden;
      }

      /* Prevent pull-to-refresh on mobile */
      body {
        overscroll-behavior-y: contain;
      }
    </style>
    <script type="module" crossorigin src="/assets/index-BFexWVS5.js"></script>
    <link rel="modulepreload" crossorigin href="/assets/rolldown-runtime-C_JxhDyB.js">
    <link rel="modulepreload" crossorigin href="/assets/app-agent-DHEP3k0d.js">
    <link rel="modulepreload" crossorigin href="/assets/app-document-DMOqloXt.js">
    <link rel="modulepreload" crossorigin href="/assets/app-preview-Ce1bg9fc.js">
    <link rel="modulepreload" crossorigin href="/assets/app-resume-BlE20C_z.js">
    <link rel="modulepreload" crossorigin href="/assets/tanstack-C-IqXR-Z.js">
    <link rel="modulepreload" crossorigin href="/assets/atom-ui-B_REHWwZ.js">
    <link rel="modulepreload" crossorigin href="/assets/date-utils-RXatGuCX.js">
    <link rel="modulepreload" crossorigin href="/assets/os-runtime-Db77RXGK.js">
    <link rel="stylesheet" crossorigin href="/assets/app-document-rptWOpCb.css">
    <link rel="stylesheet" crossorigin href="/assets/app-resume-DeKAiqmp.css">
    <link rel="stylesheet" crossorigin href="/assets/index-BFJzfqQv.css">
  
    <script id="build-manifest" type="application/json">
{
  "version": "0.1.0",
  "buildTime": 1787289628463,
  "buildHash": "f853f830b342"
}
    </script>
  <link rel="manifest" href="/manifest.webmanifest"><script id="vite-plugin-pwa:register-sw" src="/registerSW.js"></script></head>
  <body>
    <div id="root"></div>
  </body>
</html>
